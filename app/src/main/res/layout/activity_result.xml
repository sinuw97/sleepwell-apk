<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#9FBBD0"
    android:padding="16dp">

    <TextView
        android:id="@+id/prediction_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prediction Label"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/prediction_score"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prediction Score"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/suggestion_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Suggestion"
        android:textSize="14sp"
        android:paddingTop="8dp" />

    <TextView
        android:id="@+id/created_date"
        android:layout_width="87dp"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:text="created at"
        android:textSize="12sp" />


    <Button
        android:id="@+id/save_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:backgroundTint="#55679C"
        android:text="Save Prediction" />

    <Button
        android:id="@+id/back_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back"
        android:backgroundTint="#3A466A"
        android:layout_marginTop="8dp" />
</LinearLayout>
