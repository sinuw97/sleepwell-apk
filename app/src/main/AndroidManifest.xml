<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logosw"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SleepWell"
        tools:targetApi="34">

        <!-- SplashActivity -->
        <activity
            android:name="com.brydev.sleepwell.SplashScreen"
            android:theme="@style/Theme.SleepWell"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LandingPageActivity -->
        <activity
            android:name="com.brydev.sleepwell.LandingPageActivity"
            android:exported="true" />

        <!-- MainActivity (Login Activity) -->
        <activity
            android:name="com.brydev.sleepwell.MainActivity"
            android:exported="true" />

        <!-- Other activities -->
        <activity
            android:name="com.brydev.sleepwell.RegisterActivity"
            android:exported="true" />

        <activity
            android:name="com.brydev.sleepwell.HomeActivity"
            android:exported="true" />

        <activity
            android:name="com.brydev.sleepwell.SettingsActivity"
            android:exported="true" />
    </application>

</manifest>
